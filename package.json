{
  "name": "meteor-2",
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "scripts": {
    "predev": "pnpm -r test",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  },
  "dependencies": {
    "@octokit/rest": "^21.1.1"
  }
}
